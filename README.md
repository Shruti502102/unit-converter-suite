# Unit Converter Suite

## Intern Details
- **Full Name:** Shruti Srivastava
- **Intern ID:** CITS4083
- **No. of Weeks:** 4 Weeks
- **Project Name:** Unit Converter Suite
- **Project Scope:** Build a single-page utility that converts values across six measurement categories (length, weight, temperature, volume, speed, and data storage) with accurate conversion formulas, a swap function, and a live quick-reference table for common conversions.

## Overview
A multi-category unit converter covering length, weight, temperature, volume, speed, and data storage, with instant two-way conversion and a quick-reference cheat sheet for each category.

## How to Run
This is a single self-contained `index.html` file -- no build step, no dependencies, and no installation required.
1. Clone this repository.
2. Open `index.html` directly in your browser (double-click it, or right-click -> Open With -> Browser).
3. For features that call an external API (if applicable), serve the file locally instead of opening it directly:
   ```
   python3 -m http.server 8000
   ```
   then visit `http://localhost:8000` in your browser.

## Folder Structure
```
unit-converter-suite/
├── index.html      # Complete application (HTML, CSS, and JS in one file)
└── README.md        # Project documentation
```

## Tech Stack
Vanilla HTML, CSS, and JavaScript -- no frameworks, libraries, or external build tools.
